{
  "name": "map-app",
  "version": "1.0.0",
  "description": "Simple address search page",
  "scripts": {
    "test": "npx --no-install jest"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^24.0.0"
  }
}
